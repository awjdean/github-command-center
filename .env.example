# EnvironmentTokenBootstrapper reads either GITHUB_TOKEN or GH_TOKEN.
# They are equivalent for local token bootstrapping, so set only one.
GITHUB_TOKEN=ghp_your_token_here
# Optional alias if you prefer GH_TOKEN instead of GITHUB_TOKEN:
# GH_TOKEN=ghp_your_token_here
